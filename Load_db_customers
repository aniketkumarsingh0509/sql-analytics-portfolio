-- ============================================================
-- PROJECT 1: User Churn & Cohort Retention Analysis
-- Tool: SQL Server Management Studio (SSMS)
-- Dataset: Telco Customer Churn (Kaggle)
-- Author: Aniket Kumar Singh
-- ============================================================
-- STEP 1: Create Database & Table
-- ============================================================

CREATE DATABASE ChurnAnalysis;
GO

USE ChurnAnalysis;
GO

DROP TABLE IF EXISTS customers;

CREATE TABLE customers (
    customer_id       VARCHAR(20)     PRIMARY KEY,
    gender            VARCHAR(10),
    senior_citizen    INT,              -- 0 or 1
    partner           VARCHAR(5),       -- Yes/No
    dependents        VARCHAR(5),
    tenure            INT,              -- months
    phone_service     VARCHAR(5),
    multiple_lines    VARCHAR(20),
    internet_service  VARCHAR(20),
    online_security   VARCHAR(20),
    online_backup     VARCHAR(20),
    device_protection VARCHAR(20),
    tech_support      VARCHAR(20),
    streaming_tv      VARCHAR(20),
    streaming_movies  VARCHAR(20),
    contract          VARCHAR(20),      -- Month-to-month, One year, Two year
    paperless_billing VARCHAR(5),
    payment_method    VARCHAR(40),
    monthly_charges   DECIMAL(8,2),
    total_charges     DECIMAL(10,2),
    churn             VARCHAR(5)        -- Yes/No
);
GO

-- ============================================================
-- After creating the table, import the CSV:
-- Right-click ChurnAnalysis DB > Tasks > Import Flat File
-- Select your telco_churn.csv file
-- Map columns to the table above
-- ============================================================
